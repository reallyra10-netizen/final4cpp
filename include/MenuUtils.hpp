#pragma once
#include <vector>
#include <string>
#include "Student.hpp"

using std::vector; 
using std::string; 
void displayTable(const vector<Student> &students);
void printMenu(const vector<string> &items);
